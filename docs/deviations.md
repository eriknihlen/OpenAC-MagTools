# Deliberate deviations from the original Mag-Tools

A rolling table of every place this port knowingly behaves differently from
the original Decal plugin, why, and where to look. Anything not listed here
is intended to be behavior-identical. `docs/2026-09-16-port-design.md` §7
covers the separate "not applicable" list (Decal/Win32-only features with no
OpenAC equivalent) — this table is for features that DO exist here but work
slightly differently.

| Area | Original behavior | This port | Why | Where |
|---|---|---|---|---|
| Settings save | Rewrote the whole XML file on every single setting assignment. | Marks the document dirty and flushes at most once per 250 ms debounce; `Disable()` flushes synchronously. | Avoids a storage write per keystroke/toggle; semantics stay write-through (a reader always sees the new value immediately). | `src/OpenAC.MagTools/Settings/SettingsFile.cs` (`SaveDebounce`) |
| Settings account scoping | Concatenated the raw account name into an XML element name unescaped — invalid XML for any account name containing spaces or most punctuation. | `XmlConvert.EncodeName` on both the account and server name before building the scope element name. | Fixes a real original bug rather than reproducing it; the encoding is the identity transform for an ordinary alphanumeric account, so this only changes behavior for names that would otherwise have broken the document. | `src/OpenAC.MagTools/Settings/SettingsScope.cs` |
| Login banner population | Always printed a population number, defaulting to whatever Decal's proxy last cached (sometimes stale or zero). | Omits the population clause entirely when the host hasn't yet reported it (`ICharacterInfo.ServerPopulation` is `-1` until the login-time world-name message arrives), instead of printing a wrong or stale number. | The host's population figure is a one-time snapshot that may not exist yet at the exact moment the banner prints; printing nothing is preferable to printing 0 or a stale figure. | `src/OpenAC.MagTools/SessionContext.cs` (`OnLoginComplete`), `src/AcDream.Plugin.Abstractions/Automation.cs` (`ICharacterInfo.ServerPopulation`) |
| `/mt fellow create` | Drove the retail fellowship-creation dialog by clicks; the share-experience checkbox's actual state depended on whatever the user last left it at. | Always calls `Fellowship.Create(name, shareExperience: true)`. | The automation surface's `Create` takes `shareExperience` as a real parameter, but no `/mt fellow create` argument surface exists yet to pass a user choice through — `true` is a fixed default, not a rediscovered original default. Revisit if a demand for `shareExperience: false` shows up. | `src/OpenAC.MagTools/Commands/MtCommandRouter.cs` (`Fellowship`) |
| `usel <item> on <target>` (and the plain `use A on B` form) | Resolved without keyword matching: `A` is always looked up inventory-only, `B` is resolved in the command's own scope (inventory/landscape/anywhere) and is never allowed to resolve to the same object as `A`. | Ported as-is: `FindIdForName` for the source is always `searchInventory: true, searchOpenContainer: false, searchEnvironment: false`; the target search additionally passes `idToSkip` = the source's id. | Faithful port, called out here because it looks asymmetric (source is inventory-only, target is scope-wide) if you only read the target-side call. | `src/OpenAC.MagTools/Commands/MtCommandRouter.cs` (`Use`, `FindIdForName`, `idToSkip`) |
| Name-lookup search breadth (`FindIdForName`) | Two-pass lookup: an exact-name pass over inventory, then the open container, then the closest landscape object; only if that finds nothing AND partial matching was requested does the same three passes repeat with a substring match. | Ported as-is, with `idToSkip` threaded through every pass so a command that already resolved a source object never lets a target search re-select it. | Documented here because the exact/partial split and the pass order are easy to "simplify" by accident during a future refactor — don't. | `src/OpenAC.MagTools/Commands/MtCommandRouter.cs` (`FindIdForName`, `FindPass`) |
| Trade/buff-bot spam filter | Matched two composed-text regex shapes: another player's LOCAL SAY ending in `-t-"`/`-b-"` (quote included, from the wrapping `says, "..."`), or ANY non-chat line ending in `-t-`/`-b-` (no quote). | Split by structured Kind instead: `IsMine && IsChat` local speech ending in `-t-`/`-b-` (no quote — the bare `Message` never carries one), OR an `Emote`/`SoulEmote` kind specifically ending in `-t-`/`-b-`. | The composed-text "local say" half only ever matched another player's speech (never the local player's own, since that regex required the `<Tell:...>` markup `PlayerSaysLocal` uses); the "any non-chat line" half was really only ever hit by third-person emotes in practice. Reading the structured fields makes both halves precise instead of relying on regex accidents. | `src/OpenAC.MagTools/Chat/ChatFilter.cs` (`TradeBuffBotSpam`) |

| Item info ident detection (user path) | Gated `RequestId` on a real `WM_LBUTTONDOWN` message followed by a selection within 1 second. | Every `ISelectionService.Changed` IS treated as the click — the host contract has no raw click event. | The host exposes deliberate selection (a click or a select command) but not the raw mouse message the original distinguished a click from. | `src/OpenAC.MagTools/ItemInfo/IdentDetector.cs` (`UserIdentDetector`) |
| Inventory export sort: equipped ordering | Ordered equipped items within the main pack by `EquippedSlots`, a count of how many equip slots the item occupies. | Orders by `PluginInventoryItem.EquippedLocation`, the equip-slot bitmask itself, since the host has no occupied-slot count. | The host contract has no matching field; the bitmask gives a stable, deterministic order that usually but not provably matches the original. | `src/OpenAC.MagTools/Inventory/WorldObjectSorter.cs` |
| Item info line: clickable `<Tell:...>` prefix | The line's marker/rule prefix was wrapped in a `<Tell:IIDString:221112:objectId>...<\\Tell>` tag, making it a clickable name in the chat window. | Emits the plain `marker + ruleTag + " " + info` form — no wrapper. | The host's chat markup parser (`AcDream.Core.Chat.ChatTagMarkup`, gated by `ChatVM.ShouldTagSender`) only parses that tag for speech `ChatKind`s (LocalSpeech/RangedSpeech/Channel/Tell); a plugin-posted line is never one of those kinds, so the tag would render as literal text instead of a clickable link. | `src/OpenAC.MagTools/ItemInfo/ItemInfoPrinter.cs` (`PrintWithVerdict`) |
| Item info format: buffed-value math vs. active spells | `GetBuffedIntValueKey`/`GetBuffedDoubleValueKey` subtracted the `Change` of every ACTIVE spell effect on the item (buffs currently in effect), in addition to adding the item's own carried-spell `Bonus`. | The active-effect half is always a no-op for anything but the local player: `ICharacterInfo`/`PluginWorldObject.ActiveSpellIds` is empty for every object except the local player (`AppAutomationSurface.cs`, `BuildWorldObject`) — the host has no per-object active-enchantment list yet. | Reading another object's live active enchantments isn't wired up in the automation surface; until it is, buffed values for anything but your own equipped gear only reflect the carried-spell `Bonus` half. | `src/AcDream.App/Plugins/AppAutomationSurface.cs` (`BuildWorldObject`), `src/OpenAC.MagTools/ItemInfo/ItemModel.cs` (`GetBuffedInt`/`GetBuffedDouble`) |
| Loot rule verdict: ManaStone/ManaTank forward-compat | Not a deviation from the original — `Matched && Action != NoLoot` is the original's own `!result.IsNoLoot` check, restored verbatim. | The CURRENT OpenAC snapshot's shared `PluginLootAction` enum has no `ManaStone`/`ManaTank` members yet, so the classifier registry reports those two specific keep-rules as `Matched:true, Action:NoLoot` — a host artefact, not a real NoLoot verdict. `LootRuleProcessor.Classify` also checks the action's name against `"ManaStone"`/`"ManaTank"` via `Enum.TryParse` (never a direct `PluginLootAction.ManaStone`/`.ManaTank` reference, since those members don't exist in this snapshot). | OpenAC is adding the two members; this keeps the plugin compiling against both the current snapshot (the name check is a harmless no-op today) and the fixed one (the literal `Action != NoLoot` check already passes them once they're real distinct values). | `src/OpenAC.MagTools/ItemInfo/LootRuleProcessor.cs` (`Classify`, `IsManaRuleAction`) |
| Item info format: unresolvable carried-spell id | Indexed the spell table directly with no presence check — an id the table doesn't have would have thrown, taking the whole item-info line down with it. | Skips a spell id the catalog can't resolve and keeps building the rest of the line. | A missing spell id is not something this port should turn into a crashed appraisal line; every other segment already degrades gracefully (unknown skill/material/mastery names print a placeholder instead of throwing). | `src/OpenAC.MagTools/ItemInfo/ItemInfoFormatter.cs` (`AppendSpells`) |
| Combat tracker: `CombatTrackerGUIInfo` typeless-magic bug (Appendix B item 2) | `updateCombatTrackerGUIInfo` assigned `combatTrackerGUIInfo.TypelessMeleeMissile` twice (the second assignment should have been `TypelessMagic`), so the typeless-magic cell never filled in. | `CombatTrackerRows.BuildDamageRows` assigns each cell exactly once, correctly: the Typeless row's Magic column reads the typeless-magic bucket. | Fixing an obvious upstream bug, not reproducing it — nothing else depended on the broken cell staying blank. | `src/OpenAC.MagTools/Trackers/Combat/CombatTrackerRows.cs` |
| Combat tracker: `MeleeMissileGivenAttacks` regex order (Appendix B item 5) | The plain `^You [\w]+ ... $` pattern was table index 1 (right after the plain crit pattern), ahead of the Sneak-Attack/Recklessness variants. | The specific patterns (Sneak, Sneak+Reckless, Reckless, then plain-crit) are ordered before the general `^You ...` pattern. | Matches the design doc's explicit fix instruction. Verified this does NOT change which lines match any known retail shape — under .NET's default (non-Multiline) anchoring, `^You` cannot match a line that starts with the literal "Sneak Attack!"/"Recklessness!" text, so the two orderings are behaviorally identical; the reorder removes a maintenance/readability hazard rather than fixing an observed misparse. | `src/OpenAC.MagTools/Chat/CombatMessages.cs` (`MeleeMissileGivenAttacks`) |
| Combat tracker: parser subscription count | Two chat-event subscriptions (one per `StandardTracker`/`AetheriaTracker`/`CloakTracker` instance, since the original built one full tracker set per `CombatTracker`, and there are two `CombatTracker`s — current + persistent). | One subscription (`CombatTrackerHost.OnReceived`) parses each line once via the now-static, stateless `StandardTracker.Parse`/`AetheriaTracker.Parse`/`CloakTracker.Parse` and forwards the same result to both the current-session and persistent `CombatTracker` instances. | Avoids parsing every combat line twice for no behavioral difference; the original's per-instance subscription was an implementation artifact of C# events, not an intentional design point. | `src/OpenAC.MagTools/Trackers/Combat/CombatTrackerHost.cs`, `StandardTracker.cs`, `Aetheria/AetheriaTracker.cs`, `Cloaks/CloakTracker.cs` |
| Combat tracker: "is this a combat line" gate | Every tracker independently ran `Util.IsChat(e.Text)` against retail's COMPOSED display text to skip ordinary chat before pattern-matching — retail's own gate saw every chat-box line except player chat, combat or not. | Gates on the host's structured `PluginChatMessage.Kind is Combat or System` (via `ChatClassifier.Classify`), excluding `IsChat` lines, before any regex runs. On this host only the lines `CombatChatTranslator` composes (damage dealt/taken/missed/evaded) and the retail Victim/KillerNotification kill messages carry `Kind == Combat`; server-composed combat-ADJACENT text (spell damage, resists, magic-cast fizzles, aetheria surges, cloak surges, Dirty-Fighting notices) rides the host's generic `WeenieError`/text path and arrives as `Kind == System` instead. An earlier revision of this port fed only `Combat`, which matches host-composed evade/damage/kill lines but silently starved `AetheriaTracker`/`CloakTracker` (they never saw a `Combat`-kind line to parse) — this row was rewritten to record that correction. | The host already tells a plugin which lines are combat-shaped; re-deriving that from composed text would be strictly worse, and restricting to `Combat` alone drops two of the three sub-trackers this host feeds. | `src/OpenAC.MagTools/Trackers/Combat/CombatTrackerHost.cs` (`OnClassified`) |
| Combat tracker: resisted-attack chat line gap | N/A — retail always emits a chat line for a resisted/evaded attack. | `CombatState.cs`/`CombatNotificationText.cs` in the acdream host worktree were checked; the host's evade/resist text shapes are covered by `CombatMessages.FailedAttacks` as-is. A separate gap WAS found and fixed: a hostile monster's AI can use Sneak Attack/Reckless against the local player too, and neither `MeleeMissileReceivedAttacks` nor `MagicReceivedAttacks` anchored past those prefixes, so a received line carrying one had its prefix swallowed into the attacker's name by the greedy leading `targetname` group. `CombatMessages.StripReceivedPrefixes` strips `"Sneak Attack! "`/`"Reckless! "`/`"Recklessness! "` before matching the received tables. That prefix is not always at the very start of the line, either: `CombatNotificationText.DefenderLineForBodyPartText` writes `"Critical hit! "` (or `"Overpower! "`) BEFORE the Dirty Fighting text, e.g. `"Critical hit! Sneak Attack! <name> mangles ..."`, so `StripReceivedPrefixes` also pulls off a leading `"Critical hit! Overpower! "`/`"Critical hit! "`/`"Overpower! "` anchor first, strips the Dirty Fighting text behind it, then re-prepends the anchor so the received tables' own crit/overpower literal still matches. Also: `CombatNotificationText.DefenderLine` (the host's incoming-reckless-attack line) emits `"Reckless! "`, not `"Recklessness! "` — only the outgoing `AttackerLine` uses the longer word — so `StandardTracker.Parse`'s `isRecklessness` flag now accepts both spellings. | Recorded per the P4 task brief's instruction to note this as a gap rather than inventing a translator in OpenAC (which this port must not modify); the prefix-stripping and spelling-alias fixes are host-text-shape corrections within this port's own regex tables, not a change to OpenAC. | `docs/2026-09-16-port-design.md` §2.1, `src/OpenAC.MagTools/Chat/CombatMessages.cs` (`StripReceivedPrefixes`), `src/OpenAC.MagTools/Trackers/Combat/StandardTracker.cs` |
| Combat/damage list layout | A real `HudList` with per-column pixel widths (5 columns for the monster list, 5 for the damage list) — genuine multi-column alignment, numeric columns right-aligned. | Also a real multi-column list: the host's `<list>` DOES support `<column type="text" width="N">` children (see docs/plugin-ui-markup.md and this port's own Mana/Corpse/Player/Inventory/Chat tabs) — the earlier single-string-row collapse was an unjustified simplification, not a UI-surface limitation. Rebuilt as monster list columns 111/37/55/* (Name/KB's/Dmg Rcvd/Dmg Givn) and damage list columns 40/52/52/45/* (Label/Mel-Msl/Magic/StatLabel/StatValue), bound to per-column `IReadOnlyList<string>` properties (`CombatTrackerRows.MonsterRow`/`DamageRow`). Every VALUE and PERCENTAGE computation is still byte-for-byte the same math as the original. **Right-alignment itself is NOT reproduced and cells are left unpadded** — the markup's `<column>` has no alignment attribute, and the host's `UiMarkupList` draws each cell left-aligned and CLIPS it to the column's pixel width with no ellipsis (`UiMarkupList.cs`'s `DrawTextCell`/`PushClip`, confirmed against the current OpenAC source). A left-padded numeric string (an earlier revision of this row tried `PadRightAligned`, padding e.g. the 55px `Dmg Rcvd` column to 10 characters and the two 52px damage columns to 9) would have its LEAST-SIGNIFICANT digits pushed toward the clip edge and silently cut off whenever the padded width exceeds what the column's pixel width actually fits — worse than plain left-alignment, which the original port design's column widths already guarantee fit for every value this tracker produces. | The markup limitation this row originally cited (no multi-column primitive) does not exist; the multi-column list primitive was always there. Right-alignment genuinely cannot be reproduced without an alignment attribute the host doesn't have, and faking it via padding actively loses data rather than merely looking different — a real, permanent visual deviation, not an approximation. | `src/OpenAC.MagTools/Trackers/Combat/CombatTrackerRows.cs`, `src/OpenAC.MagTools/Views/magtools.xml` |
| Combat tracker: DPS-window pruning | The original's `ValueSnapShotGroup.AddSnapShot(DateTime, int, int minutesToRetain = 0)` HIDES (not overrides) the base `SnapShotGroup<int>.AddSnapShot(DateTime, int)` — the original's `CombatTracker` called the 2-argument shape, which resolves to the derived 3-argument overload with its default `minutesToRetain = 0`, and `Prune` no-ops for `minutesToRetain <= 0`. So the original's DPS snapshots were NEVER pruned, for the life of the tracker. | `CombatTracker.OnCombatEvent` explicitly passes `DpsRetentionMinutes` (60) as the third argument, so this port's snapshots ARE pruned to a 60-minute rolling window. | Unbounded per-second DPS snapshot growth for an entire play session is a real memory leak with no offsetting behavior benefit (the DPS readouts only ever look at the last 1/5/60 minutes) — this is a deliberate correction, not a faithful reproduction of the overload-shadowing bug. | `src/OpenAC.MagTools/Trackers/Combat/CombatTracker.cs` (`OnCombatEvent`), `Trackers/ValueSnapShotGroup.cs` |
| Chat classification fan-out | `ChatLogger` and `CombatTrackerHost` each subscribed to the raw chat feed independently and each called `ChatClassifier.Classify` on the same message — every delivered line was classified twice for no behavioral difference (classification is a pure function of the message and the automation surface). | `ChatClassificationDispatcher` subscribes to the raw feed exactly once, classifies once, and fans the same message + `ChatLine` out to both consumers (and any future filter-independent one, e.g. a mana-recharge listener). Both constructors take an optional dispatcher; without one they fall back to their original self-contained classify-inline behavior (used by existing unit tests that construct them directly). | Removes a redundant classification pass per delivered chat line with no behavioral change — every existing classification/gating rule is unchanged, only WHERE `Classify` runs moved. | `src/OpenAC.MagTools/Chat/ChatClassificationDispatcher.cs`, `Loggers/Chat/ChatLogger.cs`, `Trackers/Combat/CombatTrackerHost.cs`, `MagToolsPlugin.cs` |
| Combat monster-list row marker | Column 0 held a `"*"` glyph on the selected row, redundant with the list widget's own selection highlight. | Dropped — the host's `<list selectionband="true">` already highlights the selected row, so there is no separate marker column. | The host's list widget already shows selection state; a text marker column would be pure duplication. | `src/OpenAC.MagTools/Trackers/Combat/CombatTrackerRows.cs` |
| DPS window clock | Hardcoded `DateTime.UtcNow` throughout `SnapShotGroup`/`ValueSnapShotGroup`. | Takes a `TimeProvider` (default `TimeProvider.System`). | Lets a test inject a fake clock and assert exact DPS-window numbers instead of racing the wall clock; `TimeProvider.System` reproduces the original's behavior exactly when no fake is supplied. | `src/OpenAC.MagTools/Trackers/SnapShotGroup.cs`, `ValueSnapShotGroup.cs` |
| Combat tracker: `Av/Mx`/`Crits` cells when their denominator is non-positive | The GUI simply never called the corresponding `Set...` method when its guard failed, leaving that cell showing whatever it last displayed (stale, from `Clear()` or the previous selection). | Renders blank (`""`) whenever the corresponding denominator is `<= 0`. | This port rebuilds the whole row list fresh on every access rather than mutating a persistent widget — there is no "last displayed value" to fall back to, so blank is the only coherent choice. | `src/OpenAC.MagTools/Trackers/Combat/CombatTrackerRows.cs` (`BuildDamageRows`) |
| Combat tracker DPS math: zero-elapsed-window division | `GetValueDifference`/`GetDamageGivenOverTime` divide by the elapsed time since the closest historical sample with no zero guard; a query made in the same instant as the only sample divides by zero, producing `+Infinity`. | Ported as-is (no added guard) — flagged here because it looks like a bug worth "fixing" at a glance. | Self-corrects within a second or two of real play once more samples land; guarding it would be an undocumented behavior change from the original for an edge case that never persists. | `src/OpenAC.MagTools/Trackers/Combat/CombatTracker.cs` (`GetOverTime`) |
| Combat tracker storage keys | `<PluginPersonalFolder>\<Server>\<Character>.CombatTracker.xml` / `...yyyy-MM-dd HH-mm.xml` via `System.IO`. | `IPluginStorage` text keys `<Server>/<Character>.CombatTracker.xml` and `<Server>/<Character>.CombatTracker.<yyyy-MM-dd HH-mm>.xml` — same naming, forward-slash key separator (the storage abstraction's convention, matching `ChatLogFileStore`'s existing `StorageKey`). | The host's storage contract is a flat text-key store, not a real filesystem; naming otherwise matches exactly. | `src/OpenAC.MagTools/Trackers/Combat/CombatTrackerHost.cs` |
| Equipment/mana tracker: equip/unequip event granularity | Three distinct wire-level signals: `CreateObject` (add), `ChangeObject` with a `StorageChange` flag (add-or-remove on an equip-slot change), `ReleaseObject` (remove). | The host's `IEvents.ObjectChanged` has no "did the equip slot change" flag, only `Created`/`Updated`/`IdentReceived`/`Moved`/`Released`. `EquipmentTrackerHost` resyncs the WHOLE equipped set (`Items.CaptureOwnedItems().Where(IsEquipped)`, at most a few dozen items) against the tracked dictionary on every `Created`/`Updated`/`Released` notification instead of diffing one object. | The observable outcome (equip and unequip both update the tracked set promptly) matches; the three-event dance itself cannot be reproduced with the surfaces this host exposes. | `src/OpenAC.MagTools/Trackers/Equipment/EquipmentTrackerHost.cs` (`Resync`) |
| Equipment/mana tracker: item spell data source | `MyWorldObject.Spells`/`.ActiveSpells` (the carried-vs-currently-active spell id lists) came off the SAME Decal `WorldObject`, alongside its mana fields. | `PluginInventoryItem` (mana fields) and `PluginWorldObject` (`SpellIds`/`ActiveSpellIds`) are two separate host records for the same object; `EquipmentTrackedItem` stores both and joins them by object id (`Objects.TryGet`). `PluginInventoryItem.AppraisedSpellIds` was NOT used for this, since it is the appraisal-panel list, not the carried/active split the activation-state math needs. | The host splits mana-bearing fields (owned-item automation) from spell-id fields (world-object automation) across two different contracts; no single call returns both. | `src/OpenAC.MagTools/Trackers/Equipment/EquipmentTrackedItem.cs` |
| Equipment/mana tracker: activation-state "spell count" gate | `SpellCount == 0 \|\| MaximumMana == 0` → `NotActivatable`, where `SpellCount` was a Decal-native count distinct from the carried-spell id list used for the gating loop itself. | Reuses the same carried-spell id list (`World.SpellIds.Count == 0`) for both the "has no spells at all" gate and the per-spell gating loop below it. | The host exposes no separate spell COUNT field, only the id list; for every real item these are the same information, so the substitution should be observably identical for anything the tracker will ever see equipped. | `src/OpenAC.MagTools/Trackers/Equipment/EquipmentTrackedItem.cs` (`GetState`) |
| Equipment/mana tracker: per-item burn-timer re-raise | A `burnTimer` set to exactly `SecondsPerBurn` seconds per tracked item, so the GUI ticked down right when that item's mana actually dropped. | One global `Changed` re-raise driven off `EquipmentTrackerHost`'s own 500 ms (2 Hz) resync tick — no per-item scheduled timer. The replacement is specifically the tick's IDLE branch (`EquipmentTrackerHost.OnTick`) calling `Tracker.RaiseChanged()` when nothing changed enough to warrant a resync: `EquipmentTrackedItem`'s derived numbers (`CalculatedCurrentMana`, `ManaTimeRemaining`, `GetState`) are computed on demand from stored raw state and the current clock, not cached, so nothing needs a per-item timer to invalidate it — but the bound `ManaPageViewModel` only ever re-reads those getters when `EquipmentTracker.Changed` fires (P5 second fix round, MEDIUM-3: an idle tick used to raise nothing at all, so the displayed countdown visibly froze between real equip/unequip events). It is the 2 Hz re-raise that keeps the countdown moving, not the getters recomputing "for free" on some per-frame poll that does not exist. | Every derived number is computed on demand from stored raw state and the current clock rather than cached, so there is nothing a per-item timer would need to invalidate; the Mana list's bound getters recompute from the live clock every time something (a real change OR the 2 Hz idle re-raise) tells them to. | `src/OpenAC.MagTools/Trackers/Equipment/EquipmentTrackedItem.cs`, `EquipmentTrackerHost.cs` (`OnTick`) |
| Consumables/profit-loss trackers: resync trigger granularity | Diffed the ONE object that changed and updated its tracked-name's running total in place, off `CreateObject`/`ChangeObject`/`ReleaseObject`. | `ConsumablesTracker.Resync`/`ProfitLossTracker.Resync` re-scan the WHOLE owned-item set, but (P5 H1/H3 fix round, superseding this row's earlier "per-event, plus a 500 ms timer" wording) `InventoryTrackerHost` no longer calls `Resync` synchronously from every `ObjectChanged`: it waits for OWNED-object activity to go quiet for 1 s before the FIRST snapshot (H1 — the old immediate-at-`Start` priming recorded a synthetic 0→V ramp against the async post-login stream and fabricated large login-time rates, e.g. an 893.0/h Net Profit reading), then coalesces however many relevant `ObjectChanged` events land inside one 500 ms tick into at most one `CaptureOwnedItems()` capture. P5's SECOND fix round (MEDIUM-1) found the quiet-period gate itself was watching the wrong signal: `ObjectChanged` fires for every world entity (a monster spawning, another player crossing a cell boundary, a corpse decaying), not just inventory, so in a populated area the stream never truly goes quiet and priming — and every coalesced capture after it — was starved indefinitely. The gate (and the coalesced dirty flag) now only counts a change to an object `PluginWorldObject.IsOwned` reports as owned by the local player (a `Released` object can't be re-resolved to check, so it counts unconditionally); a hard 5 s `PrimeDeadline` also guarantees priming eventually happens even under continuous owned-object activity. `ConsumablesTracker.Resync` additionally skips `AddSnapShot` for any group whose recomputed total did not actually change since its last snapshot (so a capture triggered by an object outside every tracked class records nothing). `ProfitLossTracker.Resync`'s four series always exist rather than appearing/disappearing per name, so its very first call needs special handling: P5's SECOND fix round (MEDIUM-2) made it stamp a series when it has never recorded a snapshot yet (`SnapShotCount == 0`, preserving the genuine-zero-baseline case `ProfitLossTrackerTests.MmdPerHour_divides_the_rate_by_250000` depends on) OR its recomputed total actually changed — the same "only stamp on an actual change" rule as `ConsumablesTracker`, safe here specifically because of that `SnapShotCount == 0` carve-out. An unrelated equip/unequip inside a coalesced tick now stamps none of the four profit series, instead of all four with a redundant same-value snapshot. | Simpler and provably correct (no drift from a missed diff edge case); an inventory is small enough that a full rescan at 500 ms cadence is not a meaningful cost. The quiet-period priming exists specifically to stop the login-time fabrication; owning the gate on object ownership (not merely "any world event") keeps that gate meaningful in a busy area instead of becoming a no-op; the hard deadline is the backstop for when it still doesn't go quiet. | `src/OpenAC.MagTools/Trackers/Inventory/ConsumablesTracker.cs`, `InventoryTrackerHost.cs`, `Trackers/ProfitLoss/ProfitLossTracker.cs`, `Trackers/Equipment/EquipmentTrackerHost.cs` |
| Consumables tracker: per-item change-rate limiting | Re-raised `ItemChanged` off a 500 ms timer, rate-limited to once per TRACKED ITEM per second. | One `Changed` event per coalesced tick for the whole tracker (see the row above), naturally rate-limited to the 500 ms timer's own cadence, with an idle tick (nothing changed since the last one) still re-raising `Changed` without touching any history (`ConsumablesTracker.RaiseChanged`/`ProfitLossTracker.RaiseChanged`) so bound UI keeps re-reading its own time-window math. No additional per-item throttle. | This port's binding layer polls property getters (`InventoryTrackerRows.Build`) on any `Changed` rather than updating one row in place, so a per-row rate limit has no separate observable effect from a whole-tracker one. | `src/OpenAC.MagTools/Trackers/Inventory/ConsumablesTracker.cs` |
| Equipment/mana tracker: `ObjectChanged` resync coalescing | Three distinct wire-level signals per equip/unequip edge (see the equip/unequip event-granularity row above), no 500 ms timer involved at all. | (P5 H3 fix round) `EquipmentTrackerHost.Start` optionally takes the plugin's shared `TickScheduler`; when supplied, a burst of `ObjectChanged` events inside one 500 ms tick coalesces to one `CaptureOwnedItems()` capture instead of one per event. A caller that passes no scheduler (most unit tests, and any future caller that wants the old behavior) keeps the original per-event synchronous resync. (P5 second fix round, MEDIUM-1) the coalesced dirty flag only sets on a change to an object already in the tracked set OR one `PluginWorldObject.IsOwned` reports as owned (a `Released` object counts unconditionally, since it can no longer be resolved) — a monster spawning or a stranger crossing a cell boundary no longer marks the tick dirty for no reason. | The composition root can see dozens of equip/unequip-adjacent events arrive within the same frame during a gear swap; capturing the owned-item list on every single one is wasted work the 500 ms cadence already amortizes for the inventory trackers. Gating on relevance keeps that coalescing meaningful in a populated area instead of every single tick being dirty regardless of whether equipment actually changed. | `src/OpenAC.MagTools/Trackers/Equipment/EquipmentTrackerHost.cs` |
| Equipment/mana tracker: re-ident on chat cue | Each `EquipmentTrackedItem` subscribed to Decal's `ChatBoxMessage` independently and matched "The Mana Stone gives "/"is low on Mana."/"is out of Mana." lines naming its OWN world object's name, skipping the local player's own speech and any other player's quoted speech. | (P5 M5 fix round) `EquipmentTrackerHost` subscribes ONCE to the shared `ChatClassificationDispatcher.Classified` fan-out (or the raw `Chat.Received` feed when no dispatcher is supplied) and checks every tracked item's name against each delivered line, re-issuing `Objects.Identify` for every match; the same "You say, "/`says, "` speech guard is ported verbatim. | The host's classify-once dispatcher is the established pattern for filter-independent chat consumers (see the chat-classification-fan-out row above); one shared subscription checking N tracked items is equivalent to N independent subscriptions each checking one item, without N redundant chat-feed subscriptions. | `src/OpenAC.MagTools/Trackers/Equipment/EquipmentTrackerHost.cs` (`OnChatMessage`) |
| Equipment/mana tracker: unretained-item signal | Decal's `wo.Values(BoolValueKey.Retained)`, defaulting to `false` when the property is absent from the object's data. | (P5 H2 fix round) `EquipmentTracker.NumberOfUnretainedItems` tests the owned-item snapshot's `PublicFlags & PublicWeenieFlags.Retained` (`0x01000000`) bit instead of the appraisal-derived `EquipmentTrackedItem.Retained` bool (still captured from `ItemModel.RetainedKey` for any other caller, but no longer read by this method). | The appraisal-derived bool is only populated when the host's ident payload happens to carry that specific property id — absent for the common case, which silently zeroed this count for every real unretained item regardless of how many were actually equipped. `PublicFlags` carries the same bit unconditionally, with the same "absent bit means unretained" default the original's Decal accessor gave for free. | `src/OpenAC.MagTools/Trackers/Equipment/EquipmentTracker.cs` (`NumberOfUnretainedItems`) |
| Equipment/mana tracker: `SecondsPerBurn` for a non-negative rate | Ran the `Math.Ceiling(-0.2 / rate) * 5` formula for ANY non-zero rate, including a positive one (an item that gains mana over time) — which produces a negative, nonsensical "seconds per burn" the original never seems to hit in practice. | `EquipmentTrackedItem.SecondsPerBurn` returns 0 (treated the same as "does not burn") for any rate that is zero OR positive, only running the formula for a genuinely negative rate. | Deliberately more defensive than the literal original math, which the original itself never seems to exercise for a positive rate; a negative "seconds per burn" would produce nonsensical (negative) `CalculatedCurrentMana`/`ManaTimeRemaining` results downstream. Flagged here rather than silently changed, since it IS an observable difference for the (currently believed nonexistent) case of a recharging equipped item. | `src/OpenAC.MagTools/Trackers/Equipment/EquipmentTrackedItem.cs` (`SecondsPerBurn`) |
| Consumables tracker: representative item's per-class name filter | `ProcessObject` re-applies the SAME per-class name filter (Taper/Scarab substring match for `SpellComponent`, the two corrupted-essence names for `Misc`, …) both when deciding whether to track an object at all AND when counting matching inventory for that name — two passes, same predicate. | `ConsumablesTracker.Classify` applies that filter exactly once per item, and `Resync`'s per-item loop only ever calls `Classify` once — there is no second, separately-filtered counting pass. | Not a behavior change: `Classify` is a pure function of one item, and the original's second "count matching inventory" pass used the identical class+name predicate as its first "should I track this" pass — applying it once per item during a single full-inventory scan (this port's model) is equivalent to applying it once per candidate object AND once again per counted item (the original's create/change/release-driven model). Documented here because a future reader diffing this port against the original's two-pass structure could otherwise mistake the single pass for a dropped filter. | `src/OpenAC.MagTools/Trackers/Inventory/ConsumablesTracker.cs` (`Classify`, `Resync`) |
| Consumables tracker: tracked item's unit value snapshot | `TrackedInventory`'s `ItemValue` was set exactly ONCE at construction (the first sighting of that name) and never touched again — later sightings of the same name (a different stack, or the same stack after a partial trade) never revised it, even if the actual unit value had changed. | `TrackedConsumable.ItemValue` is overwritten on every `Resync` from whatever item is left as "the representative" for that group. | Documented, not changed: the original's "value fixed at first sighting" behavior is a real quirk (e.g. a Trade Note's stated value literally changes as pp is added/withdrawn, so the original's Trade Note unit value would go stale) but this port's simpler "always take the latest representative's value" was judged more useful and lower-risk than deliberately reproducing staleness. Flagged here as a genuine divergence rather than a silent one. | `src/OpenAC.MagTools/Trackers/Inventory/TrackedConsumable.cs` |
| HUD: "ID Queue" row | `CoreManager.Current.IDQueue.ActionCount` — a Decal-internal appraisal-queue depth counter. | Always blank (`""`), the original's own "nothing to show" convention. | No host equivalent exists; the appraisal queue is an internal detail of the host's own automation surface, not exposed to plugins. | `src/OpenAC.MagTools/Ui/HudUpdater.cs` |
| HUD: "Pack Slots" row | `Util.GetFreePackSlots(me)` — Decal's own computed "unused ordinary slots in the main pack" figure, reading the character's actual, possibly-augmented pack capacity. | `ICharacterInfo.MainPackFreeSlots`, a host-computed figure with the same stated definition ("unused ordinary slots in the main pack") — but the host's own implementation (`AppAutomationSurface.MainPackFreeSlots`) hardcodes the pack's capacity at 102 slots rather than reading a live `ItemSlots` value. | The host already computes and exposes this exact figure; no need to recompute it client-side from raw pack contents. The 102-slot hardcode is a host-side gap, not something this plugin can correct — flagged here so a future "why does this never reflect an augmented pack" report starts here instead of in this plugin. | `src/OpenAC.MagTools/Ui/HudUpdater.cs` (`PackSlotsRow`) |

| Item info: Material/Mastery/AttributeSet unmapped-id fallback | `MyWorldObject.cs`'s `Material`/`Mastery`/`ItemSet` accessors fall back to the BARE NUMBER (`IntValues[key].ToString(...)`) when the id isn't in the matching `Dictionaries.*Info` table. | The initial P3 commit synthesized "unknown material N" / "Unknown mastery N" / "Unknown set N" strings instead. Reverted to the original's bare-number fallback (a live appraisal of "Acid Phyntos Wasp Essence" surfaced this: property 353 = 80, outside the original's 1-11 weapon-mastery range, must render as the literal `(80)`, not `(Unknown mastery 80)`). | `src/OpenAC.MagTools/ItemInfo/ItemModel.cs` (`MaterialName`, `MasteryName`, `AttributeSetName`) |
| Item info: real-appraisal weapon/armor data source | `MyWorldObject`'s accessors for MaxDamage/EquipSkill/Variance/AttackBonus/DamageBonus/the seven protections read Decal's flat value-key cache, which Decal itself populated from whichever wire fields were available (WeaponProfile/ArmorProfile blob fields, folded into the same flat table by Decal's own unmarshalling). | `ItemModel` now prefers the host's typed `PluginItemProperties.WeaponProfile`/`ArmorProfile` (the server's real blob, not inferred from the flat Ints/Floats table) ahead of `PluginInventoryItem`'s typed fields, which in turn come ahead of the flat property table — `PluginWeaponProfile.Damage == -1` means that ONE field is unknown (not "no profile") and falls through to the next source, while the profile's other fields (WeaponSkill, WeaponOffense, DamageMod, DamageVariance) stay authoritative. `PluginArmorProfile` has no typed-inventory-field layer, so it goes straight to the flat table on a miss. Nether protection exists on the host record (`ArmorProfile.NetherMod`) but is never read — the original's segment 25 only ever had seven slots (Slash/Pierce/Bludgeon/Cold/Fire/Acid/Electric). | The host now carries the server's real appraisal blobs (added post-A4 for this exact gap: most armor/weapons never populate the flat PropertyInt/PropertyFloat table for these fields), so a real appraisal fills segments 8-12/17/25 instead of silently reading as absent. | `src/OpenAC.MagTools/ItemInfo/ItemModel.cs` (`WeaponProfile`, `ArmorProfile`, `HasInt`/`GetInt`/`HasDouble`/`GetDouble`) |

| Corpse/Player tracker: coordinate display string | `CoordsObject.ToString()` lives in the closed-source Decal SDK (`Decal.Adapter.dll`); its exact display format is not decompiled anywhere in this port's references. | `Trackers/Shared/Coords.cs` formats as `"{lat:F1}{N/S}, {lng:F1}{E/W}"` (one decimal, compass-letter sign), matching the well-known in-game coordinate convention. | Best-effort: the lat/lng MATH (`Util.GetCoords`) is confirmed verbatim from source; only the final string layout is a documented assumption. | `src/OpenAC.MagTools/Trackers/Shared/Coords.cs` |
| Corpse tracker: "released within 10 m" distance source | `WorldFilter_ReleaseObject`'s `Util.GetDistanceFromPlayer(e.Released)` reads the STILL-VALID released `WorldObject`'s live position at the exact moment of the event. | The host's `PluginObjectChange` for a `Released` object carries no position (the object has already left the object table) — `CorpseTrackerHost.OnReleased` instead computes distance from the LOCAL PLAYER's live `Navigation.Snapshot.Position` to the TRACKED corpse's own last-known stored coordinates, converted back to the same global compass-unit space via `Coords.FromLandblock`. Confirmed (not assumed): `AppAutomationSurface.ProjectNavigationPosition` (~line 1991 in the frozen A4 snapshot) computes `PluginNavigationPosition.EastWest/NorthSouth` as `((block-127)*192 + local-84)/240` -- already the same global compass units `Util.GetCoords`'s lat/lng are, and the exact units `HorizontalDistanceMeters`'s `*240` factor expects. `HostPosition.ToLandblockLocal` is the algebraic inverse, used to store the original's landblock-local-metres shape (`TrackedCorpse.LandBlock`/`LocationX`/`LocationY`) so the exported XML stays interchangeable while `Coords.FromLandblock` round-trips it back to the same compass value for this distance check. | The host contract has no "position of an object that just left the table" surface; this reconstructs an equivalent distance from data this plugin already owns (the tracked corpse's own recorded position) rather than needing the released object's live data. | `src/OpenAC.MagTools/Trackers/Corpse/CorpseTrackerHost.cs` (`OnReleased`), `src/OpenAC.MagTools/Trackers/Shared/HostPosition.cs` |

| Inventory logger: "in my container" check | `WorldFilter_CreateObject`/`ChangeObject` gated a fresh id-request on `e.New.Container == CoreManager.Current.CharacterFilter.Id` -- a DIRECT child of the player object (the main pack or an equip slot), not an item nested inside a side pack. | `InventoryLogger.OnObjectChanged` checks `PluginWorldObject.ContainerObjectId == ICharacterInfo.ObjectId` the same way. | Faithful port, called out because it means an item picked up straight into a side bag (not the main pack) does not trigger an immediate id request the way a main-pack pickup does -- matches the original's own behavior, not a broadening. | `src/OpenAC.MagTools/Loggers/Inventory/InventoryLogger.cs` (`OnObjectChanged`) |

| Idle actions: timer scheduling | Two Windows Forms timers -- a 4000 ms main timer, and a 2000 ms `timerWithChestOpen` that took over only while a container was open (and, per its own `timerWithChestOpen_Tick`, evaluates ONLY KeyDeringer while active) -- plus a scattered set of `WorldFilter.CreateObject`/`ChangeObject`/`ReleaseObject` "wake-up" triggers that restarted whichever timer was due. | `IdleActionManager` runs ONE uniform 2-second think tick whenever any of the five toggles is on, gated the same way (Peace mode, not busy) every tick; while a container is open it restricts the plan to KeyDeringer only (M3 fix), matching the original's chest-timer restriction exactly; there is no separate wake-trigger dispatcher (an unappraised Burning Sands Keyring still gets its id requested every tick while ringing/deringing is on -- H3). | The observable behavior -- react within a few seconds of the right conditions existing, and never ring/carve/fix while a container is open -- is unchanged; only the exact scheduling mechanics (and the up-to-2s-faster response while idle outside a container) differ. | `src/OpenAC.MagTools/Macros/Idle/IdleActionManager.cs` |
| Idle actions: "no chest within 10 m" source | `Util.GetClosestObject(" Chest", partial)` scanned Decal's raw WorldFilter object cache for a substring name match, then `Util.GetDistanceFromPlayer`. | `IdleActionManager` uses `INavigationAutomation.CaptureObjects()` (which carries live position) filtered the same way (`Name.Contains(" Chest")`) and `PluginNavigationPosition.HorizontalDistanceMeters`. | The host's `IWorldObjectAutomation` surface has no position; `INavigationAutomation` is the one that does, so this substitutes the equivalent live-position surface rather than reproducing Decal's specific cache. | `src/OpenAC.MagTools/Macros/Idle/IdleActionManager.cs` (`BuildSnapshot`) |
| Auto Buy/Sell: approach trigger | `WorldFilter.ApproachVendor` fired before the shop pane opened; the macro waited one `RenderFrame` for Virindi Item Tool to see the same vendor data. | Triggers on `IVendorAutomation.Opened` (the shop pane becoming the open one) with no extra frame delay -- there is no separate pre-open approach event on this host. | The host models "a vendor is ready to transact with" as one event, not two; no equivalent pre-open moment exists to hook. | `src/OpenAC.MagTools/Macros/AutoBuySell.cs` |
| Auto Buy/Sell: buy+sell as one round | `VirindiItemTool.PluginCore.BeginSellBuy(sell, buy, amount)` committed one buy and one sell as a single Item Tool operation, polled back to Idle. | Buy and sell are two separate `BuyAll()`/`SellAll()` commits, sequenced by `IVendorAutomation.TransactionCompleted` (buy first, then sell, then back to idle). | `IVendorAutomation` only exposes separate Buy/Sell commits; there is no combined "buy this and sell that" call to bind to. | `src/OpenAC.MagTools/Macros/AutoBuySell.cs` |
| Auto Buy/Sell, Auto Trade Add, Inventory Packer: "profile file not found" | Original printed the exact profile path it looked for and failed to find (`AutoBuySell.WorldFilter_ApproachVendor()`, `vendor profile file not found at: <path>`) and, for the trade adder and packer, silently no-op'd. | This port has no path to print (E-LOOT profiles are named, not filed) and cannot distinguish "profile missing" from "profile has no rules for what's on screen right now" except via `LootRuleProcessor.ProfileExists`'s existence probe -- used only by the packer (which needs it to pick between the character-scoped and Default profile names); the vendor/trade macros simply see every item come back unmatched and stop with "Nothing to Buy"/"Nothing to Sell"/"scan complete", the same observable end state as the original's silent no-op. | The host's `TryClassifyWithProfile` contract has no separate "does this profile exist" query beyond that same per-call existence signal; a per-vendor/per-partner "file not found" message would need a new host member. | `src/OpenAC.MagTools/ItemInfo/LootRuleProcessor.cs` (`ProfileExists`) |
| Auto Trade Add/Accept: "other party" resolution | `WorldFilter.EnterTrade(TraderId, TradeeId)` needed a documented workaround because the receiving side of a trade saw itself as both ids (to stop a mule auto-muling back), and `WorldFilter.AcceptTrade(TargetId)` fired for either side accepting and had to ignore `TargetId == me`. | `ITradeAutomation.Opened.PartnerObjectId`/`PartnerName` are always the other side regardless of who asked, and `PartnerTradeAccepted` only ever fires for the partner's own acceptance -- neither workaround has anything left to do. | The host's trade surface already normalizes both quirks away at the contract level. | `src/OpenAC.MagTools/Macros/AutoTradeAdd.cs`, `src/OpenAC.MagTools/Macros/AutoTradeAccept.cs` |
| Auto Trade Add, Inventory Packer: "needs id" gate | `DoesPotentialItemNeedID`/`FLootPluginQueryNeedsID` asked the loot engine's LIVE profile whether an item's un-appraised properties block a confident verdict. | Approximates with "the item has no appraisal data at all yet" (`PluginWorldObject.HasAppraisalData`) before classifying it against the NAMED profile. Retried every think (never one-shot) so a `Refused`/`Busy` identify gets another chance; an item that NEVER gains appraisal data is blacklisted for the rest of the run after the same 10-second stuck window every other id-wait/move-wait tracker in this port uses, so one permanently unidentifiable item cannot pin the completion message forever (P7 fix round M5). | E-LOOT's `NeedsIdentification`/`TryNeedsIdentification` are scoped to the classifier's live profile; there is no per-named-profile equivalent to ask "does THIS profile need this item identified", and the host gives no signal distinguishing "will never be identifiable" from "identify request just hasn't landed yet" -- the stuck-window blacklist is this port's own approximation of "give up eventually". | `src/OpenAC.MagTools/Macros/AutoTradeAdd.cs`, `src/OpenAC.MagTools/Macros/InventoryPacker.cs` |
| Looter: Salvage vs. LootSalvage (DOC CORRECTION, not a behavioral deviation) | The port design's own inventory note, read out of context ("skip IsNoLoot; skip salvage unless LootSalvage; skip anything that is neither IsKeep nor IsKeepUpTo"), reads as three SEPARATE, stacked checks -- which would make a Salvage-classified item unlootable even with LootSalvage on. | Treats "Salvage, with `LootSalvage` on" as a third accepted action alongside Keep/KeepUpTo -- this is what the original actually does; VTank's action flags are not mutually exclusive the way this host's single-valued `PluginLootAction` is, so a literal three-separate-checks reading of the inventory note's prose was simply a misreading of it, not a description of a real upstream contradiction. | No behavior differs from upstream here; this row exists only so a future reader of the inventory note does not re-introduce the "Salvage is unlootable" misreading. | `src/OpenAC.MagTools/Macros/Looter.cs` |
| Looter: id-request pass | Requested an id for EVERY item in the container still needing one, in one pass, per think. | Requests an id for the first item still needing one, one per think, and retries next think. | `docs/plugin-api.md`'s Identify section: a plugin identify can be legitimately `Refused`/`Busy` while the user has an appraisal of their own in flight; issuing a burst of requests every 100 ms risks flooding a busy appraisal queue with no way to know which ones landed. | `src/OpenAC.MagTools/Macros/Looter.cs` |
| Tinkering tab: row list lifetime | `TinkeringToolsView`'s row list was a WinForms control field that lived for the whole Decal plugin process -- there is no login/logoff hook that cleared it, so rows survived a reconnect (their object ids could go stale, but the original never noticed or cared). | `TinkeringToolsHost.Detach()` clears `_rows` outright, so a plugin `Disable()`/logoff always starts the next session's tab empty. | Every other per-session id-keyed cache in this port (the chat filter's speaker-class-by-name cache, the session-edge state itself) is cleared on logoff because object ids are not guaranteed to mean the same thing across a reconnect; carrying stale rows forward risks a delete/select acting on the wrong live object, or on nothing at all. | `src/OpenAC.MagTools/Macros/TinkeringToolsHost.cs` (`Detach`) |
| Tinkering: property-absent default | The Decal-era `MyWorldObject`/property-bag lookups the original read Workmanship/NumberTimesTinkered through defaulted an ABSENT int property to `-1`, not `0` -- `0` is itself a legitimate workmanship/tinker-count value. | `TinkeringToolsHost.OnObjectChanged` now defaults an absent `WorkmanshipKey`/`NumberTimesTinkeredKey` to `-1` the same way (was `0` before this row was added). | Matches the original's own "not present" sentinel instead of silently reinterpreting an absent property as a real zero value. | `src/OpenAC.MagTools/Macros/TinkeringToolsHost.cs` (`OnObjectChanged`) |

| Login/LoginComplete conveniences: two edges, two independent queues (updated P9 MEDIUM-4) | `LoginActions` hung two separate handlers off `CharacterFilter.Login` and the plugin's own `LoginComplete`, each queueing its own dummy-first command list, both scopes known at once at either edge. | `RunServerScope` plays the server-scoped On-Login and On-Login-Complete lists (dummy slot, then On-Login, then On-Login-Complete, one command per tick) on `SessionContext.LoginComplete`, since server scope needs only `WorldName`, which the host reports correctly at that edge. `RunCharacterScope` plays the identical shape for the character-scoped lists, but on the LATER `SessionContext.SessionReady` edge, since character scope also needs `ICharacterInfo.Name`, which the host does NOT reliably report at `LoginComplete` (defect #1, `docs/live-results.md`). Each scope gets its OWN dummy slot and runs on its OWN `QueuedCommandRunner`, so the two can be mid-drain at the same time. Cross-scope character-before-server ordering (both scopes queued together, character strictly before server, as the original had when both were known at once) is NOT preserved when the two edges land on different ticks -- server-scoped commands can now dispatch before character-scoped ones from the same login. | The host contract exposes only one in-world edge with everything known immediately (`LoginComplete`); once it turned out `ICharacterInfo.Name` specifically can still be empty there (unlike `WorldName`), gating BOTH scopes on the edge that protects the CHARACTER name would delay server-scoped work that never depended on it. `PeriodicCommands` gets the same split (`StartServerScope` / `SetCharacterScope`), but its 20-second poll interval makes the ordering question moot in practice. | `src/OpenAC.MagTools/Macros/LoginActions.cs`, `src/OpenAC.MagTools/Macros/PeriodicCommands.cs` |
| Combat tracker list column widths (visual only) | `CombatTrackerGUI`'s monster list authored `Dmg Rcvd` at 55px and the damage list's label column (`Typeless`/`Electric`, …) at 40px, in VirindiViewService's own font metrics. | Widened to 66px and 52px respectively (taking the extra pixels from each list's auto-filled last column, so nothing else shrinks), within the same 312px list width. | OpenAC's markup renderer's font metrics clip both headers at the original's exact pixel widths (found at the P8 tab-walk gate); this is a rendering-surface difference, not a retail-behavior change. | `src/OpenAC.MagTools/Views/magtools.xml` |
| Misc -> Options "Target Output Window:" label (visual only, L7) | `OutputWindowLabel` was authored at a fixed 95x30 box (room to wrap) in VirindiViewService's own font metrics. | The port's `<label>` has no wrap support, and even auto-sized the full 22-character string does not fit the 92 px available between the list (ends x=214) and the group's right edge (x=312) in this markup renderer's font. Split into two `<label>`s, "Target Output" / "Window:", stacked at y=2/y=18. | Fixes the clip without inventing wrap support the markup dialect doesn't have, and without shrinking the caption's wording. | `src/OpenAC.MagTools/Views/magtools.xml` |
| Corpse tracker: Track Fellow/Permitted Corpses (DOC CORRECTION, not a behavioral deviation, L6) | The GUI advertises `TrackFellowCorpses`/`TrackPermittedCorpses` checkboxes, but `CorpseTracker`'s own branches for them are empty upstream `// fix` stubs (Appendix B item 6 of the inventory) -- toggling either setting changes nothing observable. | `CorpseTracker` has no branch for either setting at all; the two `Setting<bool>` values are still read/written by the host (`/mt opt`, the Options page) so they round-trip, but neither is consulted by the tracking decision. | No behavior differs from upstream here; this row exists only so a future reader of `CorpseTracker`'s "See docs/deviations.md" comment finds the matching entry instead of an empty search. | `src/OpenAC.MagTools/Trackers/Corpse/CorpseTracker.cs` |
| Periodic commands: same-minute guard reproduces an upstream clock-mixing quirk (L5, not a port bug) | The original gated its "same minute never runs twice" check on `DateTime.UtcNow.Minute` (0-59, no hour/day) while computing `minutesAfterMidnight` from the UNRELATED `DateTime.Now` (local) clock -- two different clocks feeding one feature. Two further ONE-SHOT quirks fell out of the SAME guard variable's original implementation: (1) a non-nullable `int` guard defaults to `0`, so if the very FIRST evaluation of a session happens to land in UTC minute `:00`, the guard reads as "already evaluated" and that first evaluation is silently skipped; (2) the guard field was never reset across a re-login, so a quick relog landing in the same UTC minute-of-hour as the previous session's last evaluation skips ITS first evaluation too. Both self-correct on the very next 20-second poll -- a permanent skip, not a one-time miss, would matter; a one-shot miss inside a 20-second window does not. | Reproduces the CROSS-CLOCK mixing exactly: the guard compares only `GetUtcNow().UtcDateTime.Minute`; `minutesAfterMidnight` reads `GetLocalNow()`. Does NOT reproduce the two one-shot skips: `_lastEvaluatedUtcMinute` is `int?`, starting `null` (never collides with a real `0` minute value), and is explicitly reset to `null` in both `Start()` and `Stop()` (a fresh session always evaluates its first poll). | The cross-clock mixing is the original's own behavior worth preserving faithfully (M1, 2026-09-16 review) -- "fixing" it would diverge from upstream. The two one-shot skips are NOT worth deliberately reproducing: each only ever suppresses a single evaluation that self-corrects 20 seconds later, and reproducing them would mean writing a sentinel collision / a missing reset on purpose rather than porting an observable behavior (LOW-2). | `src/OpenAC.MagTools/Macros/PeriodicCommands.cs` (`_lastEvaluatedUtcMinute`) |
| Open main pack on login: mechanism, not `Items.Use(self)` (H1 resolved via OpenAC slice A6) | `Actions.UseItem(myId, 0)` opened the main pack as a side effect of "using" the player's own object. | Calls `IUiRegistry.ShowClientWindow(PluginClientWindow.Inventory)` directly instead -- `IItemAutomation.Use` on the local player's own object id is refused by this host (`IsPlayerOwned` excludes the player object itself), and OpenAC slice A6 added a direct client-window surface that reaches the same observable result (the main pack opens) without going through item-use at all. Still never swallows a refusal: logs a warning when the window does not end up visible. | The host has no "use self" route to the inventory window; the direct client-window call is the more faithful match to what the original actually achieved (the pack opening), not to the specific mechanism it used to get there. | `src/OpenAC.MagTools/Macros/OpenMainPackOnLogin.cs` |

| Queued-command dispatch: `/mt` verb boundary + trim (MEDIUM-3) | `DispatchOnLoginCommand`'s dispatch check was a plain `command.StartsWith("/mt")` with no trim of leading whitespace and no boundary after the verb -- a stored command like `" /mt test"` (leading space) would fall through to the chat-submit branch instead of the console, and `/mtx foo`/`/mtblah` would be routed into the console as a malformed `/mt` verb instead of being submitted to chat as typed. | `CommandDispatcher.TryGetMtArguments` trims leading whitespace before matching and requires `/mt` be followed by whitespace or end-of-string, so both of the above go the OTHER way from upstream: the leading-space case now correctly reaches the console, and the `/mtx`/`/mtblah` case now correctly reaches chat. | Deliberately stricter than upstream. A stored login/periodic command is user-authored text a user would reasonably expect to work whether or not they added a leading space, and a command that merely starts with the same four characters as `/mt` is far more likely to be an ordinary chat line than a malformed console verb -- reproducing the original's looser match would silently mis-route both. | `src/OpenAC.MagTools/Macros/CommandDispatcher.cs` (`TryGetMtArguments`) |
| `/mt use*` refusal message (P9) | Printed nothing on a refused `Items.Use`/`Items.Apply` call -- a resolved-but-refused command was silent, indistinguishable from the command never running at all (defect #4, `docs/live-results.md`). Success was also silent. | Still silent on `Started` (matching upstream), but now prints `Use refused: <notice-or-status>` for anything else, preferring the host's own `PluginItemCommandResult.Notice` text over the bare status name when the host supplied one. | This host's `Items.Use`/`Items.Apply` can refuse for reasons Decal's synchronous API never had (InvalidItem/InvalidTarget/Busy/Refused/Unavailable) with no other signal reaching the user; staying silent on a refusal would just reproduce the defect. | `src/OpenAC.MagTools/Commands/MtCommandRouter.cs` (`ReportUse`) |
| `/mt fellow` refusal message (P9) | Printed nothing on a refused fellowship command (create/open/close/disband/quit/recruit) -- server rejection or "no fellowship API available" looked identical to the command never running (defect #6). Accepted was, and still is, silent. | Still silent on `Accepted` (matching upstream -- the server's own roster/panel update is the confirmation), but now prints `Fellowship request refused: <status>` for `Rejected`/`Unavailable`. | Same shape as the use-refusal row: a real refusal has to reach the user somehow, but a real success must not gain a line upstream never printed. | `src/OpenAC.MagTools/Commands/MtCommandRouter.cs` (`Fellowship`'s `Report`) |
| `/mt cast[p]` refusal message (P9) | Printed nothing on a refused cast (`IMagicCommands.Cast` returning `false`) -- no `No spell named:`/`No target found named:`, no server reaction, nothing (defect #5). A `Sent` cast was, and still is, silent (the server's own reaction is the confirmation). | Calls `RequestCast` instead of the plain `Cast` bool and prints `Cast refused: <status>` for anything other than `Sent` (`UnknownSpell`/`NoTarget`/`IncompatibleTarget`/`MissingComponents`/`Unavailable`). | `RequestCast`'s richer `PluginCastRequestResult` is the only way to tell a real refusal from a silent success; without it a refused cast and a working one were indistinguishable. | `src/OpenAC.MagTools/Commands/MtCommandRouter.cs` (`Cast`, `ReportCastRequest`) |
| Clipboard-unavailable message (P9) | Both Tools -> Inventory clipboard buttons printed the completion message unconditionally -- `InventoryExporter` never checked `IPluginClipboard.TrySetText`'s return value, so a refused write (no clipboard, another process holding it) looked exactly like a real copy (defect #2). | Prints `Clipboard is unavailable; nothing was copied.` (and logs it) instead of the completion line when `TrySetText` returns `false`. | Decal's clipboard call had no failure mode worth reporting; this host's does, and silently claiming success when nothing was copied is worse than saying so. | `src/OpenAC.MagTools/Inventory/InventoryExporter.cs` (`StopThinking`) |
| `ItemIsEquippedByMe` predicate (P10, defect 9) | The original's Decal-era rule treated `EquippedSlots > 0` as sufficient except for weapons (`Slot == -1`), which ALSO required the item's container to be the player -- a Decal-specific quirk of how weapon slots were represented. | Checks `PluginInventoryItem.WielderObjectId == Character.ObjectId` for every equipped item; the container-based special case is gone entirely. | On this host `ContainerSlot == -1` for EVERY equipped item (not only weapons) and `ContainerObjectId` holds whatever container the item was equipped FROM (or 0), never the player -- the original's rule rejected every equipped item here, always exporting an empty "Clipboard Worn Equipment". `WielderObjectId` is this host's actual "who has this equipped" field. | `src/OpenAC.MagTools/Inventory/InventoryExporter.cs` (`IsEquippedByMe`) |
| Inventory export: bounded id-wait retry (P10, defect 10) | No original source for this class' retry behavior on an item that never receives id data is available in this port's references (no vendored Decal source, and the design doc doesn't specify one); the observed live behavior was request-once-then-wait-forever, which is treated here as a defect rather than an intended original design. | `InventoryExporter.Think` re-requests any still-unappraised item's id every 5 seconds, up to 3 retries; if an item still lacks id data after that it exports anyway (without that item's full info) and prints a count message, instead of waiting indefinitely. | A live gate found "Clipboard Inventory Info" printing "Copying..." forever (130+ seconds, no completion) against a single never-appraised item (defect 10) -- an unbounded wait is a worse user experience than a timed-out export missing one item's details. | `src/OpenAC.MagTools/Inventory/InventoryExporter.cs` (`Think`, `IdRetryInterval`/`MaxIdRetries`) |
| Inventory logger: deferred, polled startup dump (P10 review, HIGH-1/HIGH-3/MEDIUM-B) | Dumped synchronously on login, in the same call that decided the fresh-file-vs-existing-file branch -- no wait, no poll, no give-up. | `Start()` runs the dump immediately ONLY if the owned pack is already populated at that instant (the common reconnect case); otherwise it polls `IItemAutomation.CaptureOwnedItems()` once per second through the plugin's `TickScheduler` until the pack is non-empty, capped at 60 polls (~60s). On give-up the poll is DISPOSED outright (not just flagged) and logs a warning -- nothing is ever dumped that session, even if the pack streams in later. Once the startup capture succeeds, the SAME poll keeps running purely to refresh the last-known-good owned-item snapshot every second (skipping the refresh while `ICharacterInfo.IsInWorld` is false, so a poll landing after the session has already left the world cannot stomp the snapshot -- MEDIUM-A), so a later `Stop()` reflects mid-session loot/purchases/tinkering instead of only the login-time inventory. | The original's host (Decal) apparently always had the pack available synchronously at login; this host's pack can still be streaming in at `SessionReady` (defect 8). A bounded, low-frequency poll is the deliberate fix for that gap, and reusing it as a continuous snapshot refresh avoids adding a second timer for the same underlying "what does the player currently own" question. | `src/OpenAC.MagTools/Loggers/Inventory/InventoryLogger.cs` (`Start`, `OnSnapshotPoll`) |
| Inventory logger: `_waitingForIdData` unconditional set (P10 review, MEDIUM-7 doc correction, not a new behavioral change) | The original set its "waiting for ids" flag unconditionally whenever the fresh-file branch requested at least one id. | This port only sets `_waitingForIdData = true` when at least one item was actually missing id data (`anyMissing`); if every ident-worthy item already has id data (nothing was requested), it dumps immediately instead of waiting for an `IdentReceived` that will never arrive. | No behavior differs from the ORIGINAL's practical outcome here (in the original's world every login always had at least one un-appraised item, so the flag and the wait were never actually vacuous) -- this is a defensive branch for a case (`anyMissing == false`) the original's assumption didn't need to handle; recorded so a future reader of the `anyMissing` branch doesn't mistake it for an unexplained divergence. | `src/OpenAC.MagTools/Loggers/Inventory/InventoryLogger.cs` (`RunStartupCapture`) |
| Inventory logger: periodic partial dump while waiting for ids (defect 8b, live-gate round 4) | The original's single "waiting for ids" completion path dumped exactly once, when every ident-worthy item had id data. No original source models a partial/incremental persist. | `OnSnapshotPoll` now also calls `Dump(requestIdsIfMissing: false, ...)` once per second (guarded on the identified-id SET actually changing, LOW-1 P12 review) while `_waitingForIdData` is set, independent of whether the completion latch ever trips. | Root cause, corrected at P12 review: `AppAutomationSurface.Identify`'s `CanBeginRequest` gate (`InventoryTransactionState.CanBeginRequest => _busyCount == 0 && ...`, a client-wide single-in-flight-request gate shared with every other inventory transaction, not appraisal-specific) refuses -- `Busy`, with NO call to the underlying appraisal request at all -- every identify attempt made while one is already in flight. A burst `foreach` that called `Identify` for every missing item in one tick could therefore only ever get the FIRST accepted; the rest were silently refused. (An earlier version of this row/comment attributed this to `RuntimeInteractionTransactionState.TryRequestAppraisal` "displacing" an earlier awaiting request -- that guard exists but only fires for a User-vs-Automation origin conflict, and `CanBeginRequest` refuses the call before that guard is ever reached; the displacement theory was wrong.) With the all-or-nothing completion latch never tripping (and it never will, once even one item never appraises), nothing real ever reached `Storage` before logoff; `Stop()`'s dump then ran against a torn-down object table and fell back to the empty "unresolved" shape for every item, writing `HasIdData=false` across the board even though most items really had been identified. Persisting partial progress gives `Stop()`'s post-teardown `Combine` a real `previous` (read back from `Storage`) to merge against. HIGH-1 (P12 review) additionally fixed the burst itself: `RunStartupCapture`, `OnObjectChanged`'s per-item path, and `Dump`'s own `requestIdsIfMissing` re-identify pass all route through a paced one-at-a-time queue (`EnqueueIdentify`/`PumpIdentifyQueue`) instead of calling `Identify` in a tight loop, and only record an id into `_requestedIds` once the host's result actually confirms `Accepted` -- the old code recorded it unconditionally, permanently poisoning every item after the first in a burst against ever being retried. HIGH-A (P13 review) found the P12 fix itself still pumped once per `Created`/`IdentReceived` event, and the host raises `Created` for EVERY world entity, not just owned/ident-worthy ones -- a login stream/landblock crossing/crowded town pumped many times per frame while this class's OWN just-accepted identify was still outstanding (guaranteed `Busy`, self-inflicted), exhausting the old event-COUNT give-up (10 refusals) after roughly 11 unrelated world registrations. Fixed by (1) never attempting anything while this class's own identify is in flight (`_inFlightIdentifyId`, cleared on that id's `IdentReceived` or a time-based safety valve) and (2) making the give-up bound TEMPORAL (`MaxIdentifyRefusalSeconds`, 30 s of continuous refusal since the first one seen for that id) instead of a count, and by no longer pumping opportunistically on plain `Created` at all (only `IdentReceived` plus the 1 Hz poll plus the per-item path's own post-ownership-check pump). MEDIUM-B (P13 review), user-visible behaviour: the identify request slot is a single client-wide resource SHARED with every other MagTools feature that identifies things (the clipboard exporter, the equipment tracker, the looter, the ident-on-pickup detector) and with the user's own manual assess -- contention from any of those can still cause real, non-spurious `Busy` responses, which is why the give-up is a bound or measured in real elapsed seconds, not an instant failure. If an item's id is never actually granted within that window, this class stops requesting it for the rest of the session (logged via `IPluginHost.Log.Warn`, not chat) -- the item still appears in the dump, just without id data, unless some OTHER feature (or the user) separately identifies it later, in which case this class picks up that data passively the next time it dumps. P14 review found three further gaps in the P13 fix: MEDIUM-1, `InvalidItem` (the host's own Identify found the object no longer in the table -- sold, dropped, out of range) was being treated like ordinary contention and held the queue head for the full 30 s bound, serialised across a batch (~20 min of no progress for 40 such items) -- both `InvalidItem` and a straight `TryGet` miss now give up immediately and the SAME pump call moves on to the next queued id. MEDIUM-2, the host documents that a plugin's own accepted Identify can be displaced by a LATER one (of either origin), silently dropping its response (`AcDream.Plugin.Abstractions.LootAutomation.cs`'s `PluginAppraisalState.AwaitingObjectId` remarks) -- since accept already recorded the id into `_requestedIds`, a displaced id was never re-enqueued or warned about and the queue simply idled until the safety valve; this class now polls the shared appraisal slot (`ILootAutomation.Appraisal`) each pump and re-enqueues immediately on a detected displacement. LOW-1, the 30 s bound is only ever evaluated INSIDE a pump call; in the rare case where the owned pack never populates and `OnSnapshotPoll` disposes its own poll (HIGH-2/MEDIUM-B, P10 review), a later per-item enqueue whose identify then gets displaced would have nothing left pumping it -- a one-shot `TickScheduler.Delay(30s, ...)` is now armed whenever this class starts a clock it needs checked, independent of the periodic poll or any other event traffic. | `src/OpenAC.MagTools/Loggers/Inventory/InventoryLogger.cs` (`OnSnapshotPoll`, `PumpIdentifyQueue`, `EnqueueIdentify`, `GiveUp`, `ArmGiveUpTimer`, `_inFlightIdentifyId`) |
| `/mt vendor ...` and Auto Buy/Sell: report a host-level refusal (defect 14a, live-gate round 4) | No original source models a refused vendor pane command (Decal's Virindi Item Tool call was fire-and-forget from the plugin's perspective). | Every `/mt vendor addbuy(p)/addsell(p)/buy/sell/clearbuy/clearsell` now reports (`"<action> refused: <status/notice>"`) any `PluginVendorCommandResult` that is not `Sent`, the same shape `ReportUse` already established for `/mt use*` after defect #4; `AutoBuySell` reports the same for its own `AddToBuyList`/`AddToSellList`/`BuyAll`/`SellAll` calls and no longer advances its Buying/Selling phase on a refusal (previously it would wait forever for a `TransactionCompleted` that a refused command never sends), and its `OnTransactionCompleted` now reports `PluginVendorTransaction.Success == false` instead of treating a server-rejected trade identically to a real one. | Live round 4: `/mt vendor addbuyp a`, `addbuyp e`, `buy`, `addsellp taper`, `sell` printed nothing and nothing was bought or sold, with no way to tell a refusal from a race from a silent host bug. | `src/OpenAC.MagTools/Commands/MtCommandRouter.cs` (`ReportVendor`), `src/OpenAC.MagTools/Macros/AutoBuySell.cs` (`TryVendorCommand`, `OnTransactionCompleted`) |
| `/mt client minimize`/`/mt quit`/`/mt exit`: headless host (P13, OpenAC A10) | No original source models these on a headless host — Decal's Win32 hooks assumed a real window always existed. | On the graphical host these three drive the real `IHostWindow`. On the headless host `Minimize`/`Restore` stay at the interface's inert `Unavailable` default (there is no OS window to act on) and this router prints `Window command refused: Unavailable`; `RequestClose` is real there too — it ends only the calling plugin's own session (the same route a bot policy already uses to end its own session), not the whole headless process. | `docs/plugin-api.md` §Host window/§Headless; `src/OpenAC.MagTools/Commands/MtCommandRouter.cs` (`ReportWindow`) |
| `/mt client minimize`: Wayland confirmation caveat (P13, OpenAC A10) | Not applicable to the original (Win32-only). | `Minimize()` always reports `Unavailable` on Wayland even when the window actually minimized — the compositor protocol has no way to report iconification back to the client. This router treats `Unavailable` as "unconfirmed", prints the refusal line, and does not retry. | `docs/plugin-api.md` §Host window; `src/OpenAC.MagTools/Commands/MtCommandRouter.cs` (`ReportWindow`) |
| HUD updater: headless host never runs it (owner measurement, 2026-09-18) | Not applicable to the original (Decal's HUD status bar always had a window; there is no headless Decal). | `HudUpdater.Start` (the 1 Hz `scheduler.Every` registration that fills `HudViewModel`'s 14 rows, including a full `IWorldObjectAutomation.CaptureObjects()` landscape scan for the Players/Monsters rows) now only runs when `IPluginHost.HasUi` is true, gated at the registration site in `MagToolsPlugin.OnSessionLoginComplete` rather than inside the tick itself. A headless host previously paid the same capture cost every second for a panel it never draws (~10 MB allocated / 45 s, 3-4 MB standing private bytes, dotnet-trace on `acdream-headless`). Graphical-host behavior (rows, cadence, panel) is unchanged. | `src/OpenAC.MagTools/MagToolsPlugin.cs` (`OnSessionLoginComplete`), `src/OpenAC.MagTools/Ui/HudUpdater.cs` |

## Adding a row

Add a row in the same commit that introduces the deviation (or discovers an
existing one). Keep it to one line — point at the file, not a full
explanation; the "why" belongs in a code comment at the site itself, this
table is the index.
| Inventory logger: previous-record match (defect 15, P12) | Matched a persisted record to a live item on `Id` AND `ObjectClass` (`Inventory/InventoryLogger.cs:229` in the original); Decal's inventory only ever exposed fully populated objects, so the class could never transiently disagree. | Matches on `Id`, and on `ObjectClass` only when the live class is known: a live class of `Unknown` (the host publishes typeless manifest stubs before the create payload lands — `ClientObjectTable` equipment/container manifests) still matches, so a transient Unknown at the login dump no longer replaces an appraised record with a bare stub. A known, different class still refuses the match (recycled id guard, as the original). | Live round 5 lost 13 of 15 appraised records to exactly that transient mismatch; the id is the identity, the class is derived. | `src/OpenAC.MagTools/Loggers/Inventory/InventoryLogger.cs` (`IsSamePersistedObject`) |
